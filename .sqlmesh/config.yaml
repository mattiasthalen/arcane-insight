gateways:
  local:
    connection:
      type: spark
    
    state_connection:
      type: duckdb
      database: ./warehouse/state.duckdb

default_gateway: local

model_defaults:
  dialect: spark
  start: 2024-11-07
